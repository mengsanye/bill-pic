# 发布签名配置模板
#
# 用法：
#   1. 生成密钥库（只需做一次）：
#        keytool -genkeypair -keystore ~/keystores/billpic-release.p12 \
#          -storetype PKCS12 -alias billpic \
#          -keyalg RSA -keysize 4096 -validity 10000 \
#          -dname "CN=你的名字, OU=BillPic, O=BillPic, C=CN"
#   2. 复制本文件为 keystore.properties（已被 .gitignore 排除，不会入库），
#      填入 storeFile 的绝对路径与口令
#
# 三点注意：
#
#   · PKCS12 格式**不支持密钥库口令与密钥口令不同**，两项必须填一样的值。
#   · 密钥库建议放在仓库之外（例如 ~/keystores/）。放在仓库里即使被忽略，
#     也离 `git add -f` 只差一个参数。
#   · **务必备份密钥库与口令**。Android 只允许同一个签名密钥覆盖安装：
#     密钥丢失后发布的新版本对老用户就是「另一个应用」，他们必须先卸载
#     （转换记录一并丢失）才能装。建议密钥库与口令分别存进密码管理器。
#
# 未配置本文件时，release 会回退到 debug 签名——这样 clone 下来就能构建，
# 但这样的包无法覆盖安装他人构建的版本，不适合对外分发。

storeFile=/absolute/path/to/billpic-release.p12
storePassword=在此填写密钥库口令
keyAlias=billpic
keyPassword=在此填写密钥口令（PKCS12 下与上一项相同）
